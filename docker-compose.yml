services:
  news-agent:
    build: .
    container_name: news-agent
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      CONFIG_PATH: /data/config.json
      WEB_PORT: "8080"
    volumes:
      - ./data:/data
